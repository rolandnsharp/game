╔════════════════════════════════════════════════════════╗
║                                                        ║
║           🆘 HACKER'S CHEAT SHEET 🆘                   ║
║                                                        ║
║     (Only look if you're REALLY stuck!)                ║
║                                                        ║
╚════════════════════════════════════════════════════════╝

════════════════════════════════════════════════════════

📁 BASIC NAVIGATION COMMANDS:

ls                List files and folders
ls -a             List ALL files (including hidden ones)
cd folder-name    Go into a folder
cd ..             Go back one folder
pwd               Show where you are
cat filename      Read a file

════════════════════════════════════════════════════════

🔍 SEARCHING COMMANDS:

find . -name "filename"     Find files by name
grep "text" *.txt           Search for text in files
grep -r "text" .            Search recursively
file filename               Check what type a file is

════════════════════════════════════════════════════════

⚙️ RUNNING PROGRAMS & SCRIPTS:

chmod +x filename           Make a file executable
./filename                  Run a script
program-name                Run an installed program

════════════════════════════════════════════════════════

📦 INSTALLING PROGRAMS:

sudo apt update             Update package lists
sudo apt install name -y    Install a program
name                        Run the program

Examples:
  sudo apt install cmatrix -y
  cmatrix

  sudo apt install cowsay -y
  cowsay "Hello!"

  sudo apt install bastet -y
  bastet

  sudo apt install firefox -y
  firefox &

════════════════════════════════════════════════════════

🌐 NETWORK COMMANDS:

ip addr                     Show network interfaces
ip a                        (short version)
nmcli device wifi list      Show WiFi networks
nmcli connection show       Show connections
nmtui                       Text-based network GUI
ping -c 4 google.com        Test internet connection

Connect to WiFi:
  nmcli device wifi connect "NetworkName" password "password"

Or use the easier way:
  sudo nmtui

════════════════════════════════════════════════════════

💡 USEFUL SHORTCUTS:

TAB             Auto-complete names
UP ARROW        Previous command
CTRL+C          Stop/quit a program
CTRL+L          Clear the screen
history         See all your past commands

════════════════════════════════════════════════════════

🗺️ GAME WALKTHROUGH (SPOILERS!):

1. YOUR ROOM:
   - ls (look around)
   - cat note-on-desk.txt
   - ls -a (find hidden file)
   - cat .hidden-under-bed
   - cd ../hallway

2. HALLWAY:
   - cat mom-note.txt
   - ls -a (find secret message)
   - file basement-key (check file type)
   - chmod +x unlock-basement
   - ./unlock-basement
   - cd ../basement

3. BASEMENT:
   - cat old-computer.txt (learn about grep)
   - grep "secret" *.txt
   - find . -name "*secret*"
   - ls -a (find hidden safe)
   - cat .hidden-safe/secret-lab-access.txt
   - cd ../secret-lab

4. SECRET LAB:
   - cat install-guide.txt
   - sudo apt update
   - sudo apt install cmatrix -y
   - cmatrix (press CTRL+C to exit)
   - cat fun-tools.txt (optional cowsay, fortune, lolcat)
   - cat game-challenge.txt
   - sudo apt install bastet -y
   - bastet (play for a bit, press 'q' to quit)
   - ./server-room-access
   - cd ../server-room

5. SERVER ROOM:
   - cat network-guide.txt
   - ip a (check network)
   - cat firefox-mission.txt
   - sudo apt install firefox -y
   - ls -a (find mom's secret note)
   - cat .mom-secret-note (get WiFi password!)
   - sudo nmtui (connect to WiFi)
     OR: nmcli device wifi connect "HOME-WIFI" password "Cyber_Explorer_2026"
   - firefox & (open browser)
   - Go to any website!
   - cat victory.txt
   - cd ../cyberspace

6. CYBERSPACE:
   - cat welcome-to-cyberspace.txt
   - cat certificate
   - Enjoy your internet! 🎉

════════════════════════════════════════════════════════

Remember: The fun is in figuring things out yourself!
Only use this if you're REALLY stuck! 💪

════════════════════════════════════════════════════════
